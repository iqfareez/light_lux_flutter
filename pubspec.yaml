name: light_lux
description: A new Flutter project.

publish_to: "none"

version: 1.0.1-F+2

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # cupertino_icons: ^1.0.0
  light: ^2.1.0
  url_launcher: ^6.1.5
  auto_size_text: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
